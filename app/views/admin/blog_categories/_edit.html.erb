<div class="modal-header">
  <h5 class="modal-title" id="exampleModalLabel">Edit Blog Category</h5>
  <button type="button" class="close" data-dismiss="modal" aria-label="Close">
    <span aria-hidden="true">×</span>
  </button>
</div>
<div class="modal-body">
<%= simple_form_for(@blog_category, :url => admin_blog_category_path, :id => @blog_category.id, remote: request.xhr?, html: { class: 'form-horizontal', :multipart => true}) do |f| %>
  <%= f.input :name, label: 'Title'%>
  <%= f.input :is_active, as: :select, label: ' Is this category active?' %>
<button class="btn btn-success waves-effect waves-light float-right" type="submit" name="commit" value="Update Category" data-disable-with="Update">Update Category</button>
<% end %>
</div>
</div>
<div class="modal-footer"></div>
